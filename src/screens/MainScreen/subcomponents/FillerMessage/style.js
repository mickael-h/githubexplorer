import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  fillerCtn: {
    flex: 1,
    marginTop: 50,
    width: '75%',
    alignSelf: 'center',
  },
  fillerMessage: {
    fontStyle: 'italic',
    color: 'grey',
    fontSize: 28,
    textAlign: 'center',
  },
});

export default style;
