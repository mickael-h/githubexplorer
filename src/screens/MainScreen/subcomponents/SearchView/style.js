import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  mainView: {
    flex: 1,
  },
  repoItemInfoView: {
    marginTop: 4,
    flex: 1,
    flexDirection: 'row',
  },
});

export default style;
